@inject MdService MdService

<div class="prose prose-invert break-words hyphens-auto">
    @((MarkupString)MdService.ConvertToHtml(MD ?? ""))
</div>

@code {
    [Parameter]
    public string? MD { get; set; }
}
